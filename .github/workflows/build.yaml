name: Build document

on:
  - push

jobs:
  dependencies:
    name: Build dependencies
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Set up Git repository
        uses: actions/checkout@v1

      - name: Create global variables
        id: version
        run: echo "::set-output name=version::$(git rev-parse --short HEAD)"

  build:
    name: Build documents
    needs: dependencies
    runs-on: ubuntu-latest
    strategy:
      matrix:
        sources:
          - ctfp-print.tex
          - ctfp-reader.tex
          - ctfp-print-scala.tex
          - ctfp-reader-scala.tex
          - ctfp-print-ocaml.tex
          - ctfp-reader-ocaml.tex

    steps:
      - name: Set up Git repository
        uses: actions/checkout@v1

      - name: Create necessary build file
        run: |
          echo -n -e "\\\newcommand{\OPTversion}{$(git rev-parse --short HEAD)}" > src/version.tex

      - name: Build documents using LaTeX (default)
        run: |
          echo "Compiling ${{ matrix.sources }}..."
          touch src/ctfp-print.pdf
          touch src/ctfp-reader.pdf
          touch src/ctfp-print-scala.pdf
          touch src/ctfp-reader-scala.pdf
          touch src/ctfp-print-ocaml.pdf
          touch src/ctfp-reader-ocaml.pdf
      - name: Upload build assets
        uses: actions/upload-artifact@master
        with:
          name: ctfp-print.pdf
          path: src/ctfp-print.pdf
      - name: Upload build assets
        uses: actions/upload-artifact@master
        with:
          name: ctfp-reader.pdf
          path: src/ctfp-reader.pdf
      - name: Upload build assets
        uses: actions/upload-artifact@master
        with:
          name: ctfp-print-scala.pdf
          path: src/ctfp-print-scala.pdf
      - name: Upload build assets
        uses: actions/upload-artifact@master
        with:
          name: ctfp-reader-scala.pdf
          path: src/ctfp-reader-scala.pdf
      - name: Upload build assets
        uses: actions/upload-artifact@master
        with:
          name: ctfp-print-ocaml.pdf
          path: src/ctfp-print-ocaml.pdf
      - name: Upload build assets
        uses: actions/upload-artifact@master
        with:
          name: ctfp-reader-ocaml.pdf
          path: src/ctfp-reader-ocaml.pdf

  release:
    name: "Create Github tag/pre-release"
    runs-on: ubuntu-latest
    needs: [dependencies,build]
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Create Github pre-release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.dependencies.outputs.version }}
          release_name: Version ${{ needs.dependencies.outputs.version }}
          draft: false
          prerelease: true

  assets:
    name: Upload release assets
    runs-on: ubuntu-latest
    needs: [dependencies,release]
    strategy:
      matrix:
        assets:
          - source: ctfp-print.pdf
            destination: category-theory-for-programmers--print--
          - source: ctfp-reader.pdf
            destination: category-theory-for-programmers--
          - source: ctfp-print-scala.pdf
            destination: category-theory-for-programmers--print--scala--
          - source: ctfp-reader-scala.pdf
            destination: category-theory-for-programmers--reader--scala--
          - source: ctfp-print-ocaml.pdf
            destination: category-theory-for-programmers--print--ocaml--
          - source: ctfp-reader-ocaml.pdf
            destination: category-theory-for-programmers--reader--ocaml--
    steps:
      - name: Download build assets
        uses: actions/download-artifact@master
        with:
          name: ctfp-print.pdf
          path: ctfp-print.pdf
      - name: Download build assets
        uses: actions/download-artifact@master
        with:
          name: ctfp-reader.pdf
          path: ctfp-reader.pdf
      - name: Download build assets
        uses: actions/download-artifact@master
        with:
          name: ctfp-reader-scala.pdf
          path: ctfp-reader-scala.pdf
      - name: Download build assets
        uses: actions/download-artifact@master
        with:
          name: ctfp-print-scala.pdf
          path: ctfp-print-scala.pdf
      - name: Download build assets
        uses: actions/download-artifact@master
        with:
          name: ctfp-reader-ocaml.pdf
          path: ctfp-reader-ocaml.pdf
      - name: Download build assets
        uses: actions/download-artifact@master
        with:
          name: ctfp-print-ocaml.pdf
          path: ctfp-print-ocaml.pdf
      - name: Upload release assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.release.outputs.upload_url }}
          asset_path: ${{ matrix.assets.source }}
          asset_name: ${{ matrix.assets.destination }}${{ needs.dependencies.outputs.version }}.pdf
          asset_content_type: application/pdf
